# ─────────────────────────────────────────────────────────────────
# Root requirements.txt — used by Vercel's Python runtime.
# Vercel picks this file up automatically for the api/ function.
# ─────────────────────────────────────────────────────────────────

# Vercel ASGI adapter (bridges FastAPI ↔ AWS Lambda / Vercel runtime)
mangum>=0.17.0

# ── Web framework ────────────────────────────────────────────────
fastapi>=0.109.2
uvicorn>=0.27.1

# ── Data validation & settings ──────────────────────────────────
pydantic>=2.6.1
pydantic-settings>=2.2.1
python-dotenv>=1.0.1
python-multipart>=0.0.9

# ── Auth & security ─────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0

# ── Database ─────────────────────────────────────────────────────
# SQLite (default, ephemeral on Vercel — good for demos).
# For a persistent DB set APP_DATABASE_URL to a PostgreSQL URL and
# add `asyncpg` + `databases` to this file.
aiosqlite>=0.20.0
sqlalchemy>=2.0.0

# ── Google AI ───────────────────────────────────────────────────
google-generativeai>=0.3.2
google-genai>=0.1.0

# ── Document processing ─────────────────────────────────────────
pypdf>=4.0.1

# ── Rate limiting ───────────────────────────────────────────────
slowapi>=0.1.9

# ── Redis (optional — used for OTP & chat memory) ───────────────
# If REDIS_HOST is not set the app falls back to in-memory storage.
redis>=5.0.3

# ── ML / Vector search ──────────────────────────────────────────
# NOTE: faiss-cpu + sentence-transformers are large (~500 MB).
# They work on Vercel Pro (1 GB limit) but may exceed the free-tier
# 250 MB compressed function limit.
# If you hit size limits, either:
#   a) Upgrade to Vercel Pro, or
#   b) Move only the backend to Railway / Render / Fly.io and point
#      VITE_API_URL in Vercel to that external URL.
faiss-cpu
numpy>=1.26.4
sentence-transformers>=2.3.1
rank_bm25>=0.2.2
nltk>=3.8.1
scikit-learn>=1.4.1.post1
